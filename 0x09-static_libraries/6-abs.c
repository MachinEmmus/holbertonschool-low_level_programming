/**
 * _abs - Check if a number is lowerr than 0
 * @x: The number to be checked
 *
 * Return: x if the number is abs.
 */

int _abs(int x)
{
if (x < 0)
{
x = x * -1;
return (x);
}
else
{
return (x);
}
}
